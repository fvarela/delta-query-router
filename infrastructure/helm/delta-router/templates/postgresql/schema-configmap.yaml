apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "delta-router.postgresql.fullname" . }}-schema
  labels:
    {{- include "delta-router.labels" . | nindent 4 }}
data:
  schema.sql: |-
    {{- .Files.Get "files/schema.sql" | nindent 4 }}