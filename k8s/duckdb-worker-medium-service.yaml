apiVersion: v1
kind: Service
metadata:
  name: duckdb-worker-medium
  labels:
    app: duckdb-worker-medium
    duckdb-tier: medium
spec:
  type: ClusterIP
  ports:
  - port: 8002
    targetPort: 8002
    protocol: TCP
    name: http
  selector:
    app: duckdb-worker-medium
